<!-- Laptop mode (default). Paste at the top of every scheduled-task prompt. The task must have "Require this computer" on and "Work in a folder" = the vault folder. Replace the placeholders. -->

You are running an Open Stanley scheduled job on the user's own computer (the `open-stanley` plugin is installed; start by loading its `stanley` skill, which holds the shared rules and the paths of the bundled scripts under `skills/stanley/scripts/`). Vault: the connected folder `{{VAULT_PATH}}` (`export STANLEY_VAULT=<that path>`); plain files, edit in place, nothing to mirror or push. The scripts are in `$STANLEY_VAULT/.stanley-scripts/` (run them with `python3`); if that folder is missing or its `VERSION` differs from the plugin's, install them per the `stanley` skill step 1a (copy from the plugin into `/mnt/user-data/outputs/`, one `device_commit_files` call into the vault), and never search the device for the plugin. Read `my-human.md`, `strategy.md`, `voice.md`, `instructions.md`, `learn/rules.md`, and anything in the vault's `skills/` folder. Probe which MCPs are connected per `skills/stanley/references/capabilities.md`; Claude in Chrome is available, so every LinkedIn and X read, analytics pull and comment draft happens in this run, nothing is queued. Run `stanley-vault run-start {{JOB}} --promised "{{PROMISE}}"` first, then `stanley-vault missed`: if it lists a job marked `catch-up:yes` that is not this one, do that job too after your own (its instructions are `templates/scheduled-tasks/<job>.md` in the plugin), with its own run-start/run-end, and say in the report that it was a catch-up; if you cannot, name the missed job. `stanley-audit selftest` once before drafting anything, and `stanley-vault run-end <id> --delivered "..." --shown N --queued 0 --browser yes` last. Deliver the result in this session's output as the final message. Even if you find nothing, say what you checked and what you found. Never post to LinkedIn or X unless the item is marked auto-approve in strategy.md or the user says yes in this session. Never quote a number you did not read from a page or file this session. Do not open more than 25 posts per run; read LinkedIn with `get_page_text`, and if a page returns nothing, dump the extracted text into `document.body` as an `<article>` via the javascript tool and read again.
