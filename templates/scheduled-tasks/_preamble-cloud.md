<!-- Cloud mode only (runs with the laptop closed; MCPs, no browser). Paste at the top of every scheduled-task prompt. Replace the placeholders. Laptop mode uses _preamble-laptop.md instead. -->

You are running an Open Stanley scheduled job. Vault: mirror the Google Drive folder 'stanley-vault' (id {{FOLDER_ID}}) per skills/stanley/references/vault-backends.md: pull only the files this job needs, all downloads in one turn, `stanley-vault snapshot`, work, `stanley-vault changed`, then create each changed file in its subfolder (disableConversionToGoogleType) and trash the old copy. Read `my-human.md`, `strategy.md`, `voice.md`, `instructions.md`. Probe which MCPs are connected per `skills/stanley/references/capabilities.md`. Run `skills/stanley/scripts/stanley-vault run-start {{JOB}} --promised "{{PROMISE}}"` first and `run-end ID --delivered "..." --shown N --queued N --browser no` last (the three flags make `runs.jsonl` queryable: a quiet run with `shown 0` and `browser no` is not the same as a bad day), then push the changed files back. Deliver the result via {{NOTIFY: e.g. "a Slack DM to @nazar" | "a Telegram message" | "the session output"}}. Even if you find nothing, send the message and say what you checked. Never post to LinkedIn or X in this job unless the item is marked auto-approve in strategy.md.

This is a cloud run: you have the user's MCPs and no browser. Do the MCP-reachable part here (Pam/Slack/Gmail/Notion/Drive mining, drafting, audit, X via the official X MCP or a posting MCP, analytics from Buffer/X MCP). Park anything that needs LinkedIn's UI or an X reply box in the local queue with `skills/stanley/scripts/stanley-vault queue-add` and an expiry (comments: 24h; scans: 12h; analytics: 72h). The user's laptop drains the queue inside its window (default 08:00–23:00 local) via the SessionStart hook or the launchd job; say in your message what you queued.

Speed. Issue every download in a single turn as parallel calls and pull only the job's files. Run `stanley-audit selftest` once per run before drafting anything; it takes a second and proves the linter works in this session.

